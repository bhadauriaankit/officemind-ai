# ---------------------------------------------------------------------
# OfficeMind AI - Environment Configuration Template
# Copy to `.env` in the same directory and fill in real secrets.
# NEVER commit the real `.env` file. It is gitignored.
# ---------------------------------------------------------------------

# PostgreSQL
POSTGRES_DB=officemind
POSTGRES_USER=officemind_app
POSTGRES_PASSWORD=P0/abWs+aOaAYlZz2ODC2D1aHf01nf10

# Redis
REDIS_PASSWORD=uOfDBO3YfZIxw6ukaHCVlenHJzKuxQ6R

# MinIO
MINIO_ROOT_USER=officemind_minio_admin
MINIO_ROOT_PASSWORD=E045QYBliiRYPDfAaMawZIw/0to+LktT

# Qdrant (no auth by default in dev; enable API key in prod overlay)
QDRANT_API_KEY=

# Kafka
KAFKA_BOOTSTRAP_SERVERS=kafka:29092

# Spring Boot
SPRING_PROFILES_ACTIVE=docker
JWT_ISSUER_URI=http://keycloak:8081/realms/officemind

# Frontend
VITE_API_BASE_URL=http://localhost:8080/api/v1
KEYCLOAK_ADMIN_PASSWORD=vNo2oOraVWXqDY9/yxr4oS9bqUToIxcK


